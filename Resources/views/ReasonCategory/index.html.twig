{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ReasonCategory list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Isactive</th>
                <th>Isblapa</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('reasoncategory_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.isActive }}</td>
                <td>{{ entity.isBLAPA }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('reasoncategory_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('reasoncategory_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('reasoncategory_create') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
